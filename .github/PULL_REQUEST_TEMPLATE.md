## Summary

What changed and why.

## Changes

-

## Testing

How you tested these changes.

## Checklist

- [ ] `npx tsc --noEmit` passes
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] i18n keys added to both `en.json` and `fr.json` (if UI changes)
- [ ] No `any` types introduced
- [ ] Follows existing code patterns
